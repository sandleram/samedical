# Tasks — mod-db (DB (utilitário))

> **Onda:** F · **ACL:** `db`

## Backlog

- [x] Spec / plan / review
- [x] Domain `DbSettingsInterface` + Application `GetDbIndex` + Interfaces
- [x] Binding + rota `/admin/db`
- [x] Blade sanitizado (sem credenciais)
- [x] Shim Http deprecated
- [x] Feature test root
- [x] Smoke Docker `php artisan test` (`OndaFIntegrationRoutesTest` `/admin/db`)

## Deferred

- [ ] lista / gerencial / medico / rh / CRUD legado

## Entrega

- [ ] PR com link `docs/features/mod-db/`
